# ============================================================================
# Literature Review RAG System - Production Dependencies (Slim)
# ============================================================================
# This file excludes PyTorch/CUDA for smaller Docker images when using
# OpenAI embeddings. Use requirements.txt for local development with GPU.

# Core RAG dependencies (without torch/sentence-transformers)
numpy<2.0.0
chromadb==0.4.24

# FastAPI server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-multipart>=0.0.6

# LLM Integration (Groq - FREE tier with Llama 3.3 70B)
groq>=0.4.0

# OpenAI Embeddings (primary embedding provider for production)
openai>=1.0.0
langchain-openai>=0.1.0

# Configuration
pyyaml>=6.0

# PDF processing
pymupdf>=1.23.0
pypdf>=3.17.0

# LangChain utilities (minimal)
langchain>=0.1.0
langchain-community>=0.0.10

# Text processing
tqdm>=4.65.0

# BM25 for hybrid search
rank-bm25>=0.2.2

# Utilities
python-dotenv>=1.0.0

# AWS S3 Storage
boto3>=1.34.0

# Redis (optional)
redis>=5.0.0

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# OCR (optional - only if processing scanned PDFs)
# pytesseract>=0.3.10
# pdf2image>=1.16.0
